package com.tl.module3;

public class TestCircle {
    public static void main(String[] args) {
        Circle circle = new Circle(1.0);
        System.out.println(circle.Area());

        // 1. 参数变量 2. 局部变量 3. 静态变量 4. 对象变量（成员变量）
        // 1. 参数变量：在方法中定义的变量，用于接收外部传递的参数
        // 2. 局部变量：在方法中定义的变量，用于存储方法执行过程中需要的临时数据
        // 3. 静态变量：在类中定义的变量，用于存储类的所有对象共享的值
        // 4. 对象变量（成员变量）：在类中定义的变量，用于存储对象的属性值
        // 注意：对象变量（成员变量）的值是对象的引用，而不是对象的值


    }
}